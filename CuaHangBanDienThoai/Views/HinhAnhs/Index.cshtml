@model IEnumerable<CuaHangBanDienThoai.Models.HinhAnh>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Hình ảnh
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaSP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <img src="@item.HinhAnh1" height="200"/>
         
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaSP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHinhAnh }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHinhAnh }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHinhAnh })
        </td>
    </tr>
}

</table>
